// import { fromJS } from 'immutable';
// import { makeSelectContactsDomain } from '../selectors';

// const selector = makeSelectContactsDomain();

describe('makeSelectContactsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
